# Discord Role Sync Bot

Бот для автоматической синхронизации ролей между несколькими Discord серверами и одним главным сервером.

## Описание

Этот бот позволяет пользователям на главном сервере получать роли на основе их ролей на других серверах. Пользователь нажимает кнопку синхронизации, и бот автоматически проверяет его роли на всех подключенных серверах и назначает соответствующие роли на главном сервере.

## Возможности

### ✅ Полностью реализовано

**Ядро синхронизации:**
- Автоматический маппинг ролей между серверами
- Параллельные запросы к Discord API для оптимальной производительности
- Обратный индекс для O(1) проверки целевых ролей
- Проверка прав бота перед назначением ролей
- Детальное логирование всех операций

**Интерфейс:**
- Кнопка синхронизации в Discord чате
- Красивые embed-сообщения с результатами
- Прогресс-индикаторы для длительных операций

**Автоматизация:**
- Мониторинг изменений ролей в реальном времени
- Автоматическая синхронизация при изменениях

**Команды:**
- `/sync` - синхронизация ролей пользователя
- `/sync_all` - массовая синхронизация всех пользователей
- `/stats` - статистика работы бота
- `/mappings` - просмотр маппингов ролей
- И другие административные команды

**База данных:**
- SQLite с 5 таблицами для хранения данных
- Кеш маппингов для быстрого доступа
- История всех операций синхронизации
- Агрегированная статистика

## Структура проекта

```
saes-discord-bot/
├── bot/
│   ├── main.py                 # Инициализация бота
│   ├── config.py               # Загрузка конфигурации
│   ├── cogs/                   # Модули функциональности
│   │   ├── sync_button.py      # Обработчик кнопки синхронизации
│   │   ├── role_monitor.py     # Мониторинг изменений ролей
│   │   ├── admin_commands.py   # Административные команды
│   │   └── stats_commands.py   # Команды статистики
│   ├── core/                   # Ядро синхронизации
│   │   ├── sync_engine.py      # Движок синхронизации (оптимизирован)
│   │   ├── role_mapper.py      # Маппинг ролей с обратным индексом
│   │   └── permissions.py      # Проверка прав бота
│   ├── database/
│   │   ├── models.py           # Схема БД (5 таблиц)
│   │   └── operations.py       # CRUD операции
│   ├── ui/                     # UI компоненты
│   │   ├── buttons.py          # Discord кнопки
│   │   └── embeds.py           # Embed сообщения
│   └── utils/
│       ├── errors.py           # Кастомные исключения
│       ├── logger.py           # Система логирования
│       └── validators.py       # Валидаторы
├── config/
│   ├── config.yaml             # Основная конфигурация
│   └── role_mappings.json      # Маппинг ролей между серверами
├── data/                       # Создается автоматически
│   └── bot.db                  # SQLite база данных
├── logs/                       # Создается автоматически
│   └── bot.log                 # Лог файлы с ротацией
├── requirements.txt            # Python зависимости
├── .env.example                # Пример переменных окружения
├── .gitignore
└── run.py                      # Точка входа
```

## Установка и настройка

### 1. Установка зависимостей

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать виртуальное окружение
# На Windows:
venv\Scripts\activate
# На Linux/Mac:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
copy .env.example .env
```

Откройте `.env` и укажите токен вашего бота:

```env
DISCORD_BOT_TOKEN=ваш_токен_бота_здесь
```

Опционально можно добавить:
```env
ERROR_WEBHOOK_URL=https://discord.com/api/webhooks/...  # Webhook для отправки ошибок
```

### 3. Создание Discord бота

1. Перейдите на https://discord.com/developers/applications
2. Создайте новое приложение
3. Перейдите в раздел "Bot" и создайте бота
4. Скопируйте токен и вставьте в файл `.env`
5. Включите следующие Privileged Gateway Intents:
   - SERVER MEMBERS INTENT
   - MESSAGE CONTENT INTENT

### 4. Настройка конфигурации

Откройте `config/config.yaml` и укажите:
- `main_server_id` - ID вашего главного сервера
- `sync_button_channel_id` - ID канала для кнопки синхронизации
- `admin_role_ids` - ID ролей администраторов
- `log_channel_id` - ID канала для логов (опционально)

Откройте `config/role_mappings.json` и настройте маппинг ролей:
```json
{
  "mappings": [
    {
      "id": "unique_id",
      "source_server_id": "ID_источника",
      "source_role_id": "ID_роли_источника",
      "target_server_id": "ID_главного_сервера",
      "target_role_id": "ID_целевой_роли",
      "description": "Описание маппинга",
      "enabled": true
    }
  ]
}
```

### 5. Приглашение бота на серверы

Создайте ссылку приглашения со следующими правами:
- View Channels
- Send Messages
- Embed Links
- Read Message History
- Manage Roles
- View Server Members

URL для приглашения:
```
https://discord.com/api/oauth2/authorize?client_id=ВАШ_CLIENT_ID&permissions=268438544&scope=bot%20applications.commands
```

Замените `ВАШ_CLIENT_ID` на Client ID вашего бота.

## Запуск

```bash
python run.py
```

## База данных

Бот использует SQLite для хранения данных. База создается автоматически при первом запуске в директории `data/bot.db`.

### Таблицы:

1. **sync_state** - Состояние синхронизации пользователей
2. **role_assignments** - История назначения ролей
3. **sync_logs** - Детальные логи операций
4. **statistics** - Агрегированная статистика
5. **role_mapping_cache** - Кеш маппингов для быстрого доступа

## Логирование

Логи сохраняются в директории `logs/bot.log` с автоматической ротацией файлов (10MB, 5 резервных копий).

Уровни логирования:
- DEBUG - Детальная диагностика
- INFO - Общие операции
- WARNING - Предупреждения
- ERROR - Ошибки
- CRITICAL - Критические ошибки

## Использование

После запуска бот автоматически:
1. Создаст базу данных в `data/bot.db`
2. Загрузит маппинги ролей из конфигурации
3. Разместит кнопку синхронизации в указанном канале

Пользователи смогут:
- Нажать кнопку "🔄 Синхронизировать роли" для синхронизации
- Получить детальную информацию о назначенных/удаленных ролях

Администраторы смогут:
- Использовать команды `/sync`, `/stats`, `/mappings`
- Просматривать логи в канале логов
- Управлять маппингами ролей

## Безопасность

- Никогда не коммитьте файл `.env` в git
- Храните токен бота в безопасности
- Регулярно проверяйте права бота на серверах
- Делайте резервные копии базы данных

## Лицензия

MIT

## Автор

Разработано с помощью Claude Code
