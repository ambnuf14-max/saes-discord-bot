# Руководство по устранению проблем

## Общие проблемы и решения

### 1. Бот не запускается

#### Ошибка: "ModuleNotFoundError: No module named 'discord'"

**Причина**: Не установлены зависимости

**Решение**:
```bash
pip install -r requirements.txt
```

Если используете виртуальное окружение:
```bash
# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Затем установите зависимости
pip install -r requirements.txt
```

#### Ошибка: "ConfigurationError: Токен Discord бота не найден"

**Причина**: Отсутствует или неправильно настроен .env файл

**Решение**:
1. Убедитесь что файл `.env` существует в корневой директории проекта
2. Проверьте содержимое:
   ```env
   DISCORD_BOT_TOKEN=ваш_реальный_токен
   ENVIRONMENT=development
   ```
3. Убедитесь что нет пробелов вокруг знака `=`
4. Проверьте что токен скопирован полностью

**Как получить токен**:
1. Перейдите на https://discord.com/developers/applications
2. Выберите ваше приложение (или создайте новое)
3. Перейдите в раздел "Bot"
4. Нажмите "Reset Token" и скопируйте новый токен
5. Вставьте в .env файл

#### Ошибка: "yaml.scanner.ScannerError"

**Причина**: Синтаксическая ошибка в config.yaml

**Решение**:
1. Проверьте отступы (используйте пробелы, не табы)
2. Убедитесь что строки с двоеточием имеют пробел после него:
   ```yaml
   # Правильно:
   bot:
     main_server_id: 123456789

   # Неправильно:
   bot:
   main_server_id:123456789
   ```
3. Используйте онлайн валидатор YAML для проверки: https://www.yamllint.com/

### 2. Бот подключается, но не отвечает на команды

#### Проблема: Бот онлайн, но игнорирует команды

**Решение 1**: Проверьте префикс команд

В `config/config.yaml` проверьте:
```yaml
bot:
  command_prefix: "!"
```

Используйте команды с этим префиксом: `!roleadmin list_mappings`

**Решение 2**: Проверьте права бота

Бот должен иметь права:
- View Channels (читать каналы)
- Send Messages (отправлять сообщения)
- Read Message History (читать историю)

**Решение 3**: Проверьте Intent'ы

Убедитесь что в Developer Portal включены:
1. `MESSAGE CONTENT INTENT` (для префиксных команд)
2. `SERVER MEMBERS INTENT` (для доступа к участникам)

Путь: Discord Developer Portal → Your App → Bot → Privileged Gateway Intents

### 3. Ошибки синхронизации ролей

#### Ошибка: "403 Forbidden: Missing Permissions"

**Причина**: У бота недостаточно прав для управления ролями

**Решение**:

1. **Проверьте роль бота на сервере**:
   - Настройки сервера → Роли
   - Найдите роль бота
   - Убедитесь что включено право "Manage Roles"

2. **Проверьте иерархию ролей**:
   - Роль бота должна быть выше управляемых ролей
   - Переместите роль бота вверх в списке ролей

3. **Используйте команду проверки**:
   ```
   !roleadmin check_permissions
   ```

#### Ошибка: "User not found on main server"

**Причина**: Пользователь не является участником главного сервера

**Решение**:
- Убедитесь что пользователь присоединился к главному серверу (main_server_id в config.yaml)
- Проверьте что ID сервера правильный

#### Проблема: Роли не синхронизируются автоматически

**Решение 1**: Проверьте что автосинхронизация включена

В `config/config.yaml`:
```yaml
sync:
  auto_sync_enabled: true
```

**Решение 2**: Проверьте статус автосинхронизации

Выполните команду:
```
!toggle_autosync
```

Если написано "включена" - все в порядке.

**Решение 3**: Проверьте маппинги

Выполните:
```
!roleadmin list_mappings
```

Убедитесь что:
- Маппинг существует для роли
- Маппинг включен (`enabled: true`)
- ID серверов и ролей правильные

**Решение 4**: Проверьте очередь синхронизации

```
!sync_queue
```

Если очередь пуста, но роли меняются - проверьте логи.

#### Проблема: Некоторые роли синхронизируются, другие нет

**Причина**: Иерархия ролей или отсутствующие маппинги

**Диагностика**:

1. Проверьте логи синхронизации:
   ```
   !rolestats logs 20
   ```

2. Проверьте маппинги:
   ```
   !roleadmin list_mappings
   ```

3. Проверьте иерархию ролей на главном сервере:
   - Роль бота должна быть выше целевых ролей

4. Попробуйте ручную синхронизацию конкретного пользователя:
   ```
   !roleadmin sync_user USER_ID
   ```

### 4. Проблемы с кнопкой синхронизации

#### Проблема: Кнопка не реагирует на нажатия

**Решение 1**: Пересоздайте кнопку

```
!setup_sync_button
```

**Решение 2**: Проверьте что бот перезапущен после создания кнопки

Persistent views регистрируются при запуске бота. Если кнопка была создана до запуска, пересоздайте её.

**Решение 3**: Проверьте логи

Посмотрите в `logs/bot.log` на наличие ошибок при нажатии.

#### Ошибка: "This interaction failed"

**Причина**: Бот не успел ответить в течение 3 секунд

**Решение**:
- Проверьте производительность сервера (медленный CPU/RAM)
- Проверьте подключение к Discord API
- Уменьшите количество серверов для синхронизации
- Проверьте логи на наличие ошибок

### 5. Проблемы с базой данных

#### Ошибка: "database is locked"

**Причина**: Несколько процессов пытаются получить доступ к БД

**Решение**:
1. Убедитесь что запущен только один экземпляр бота
2. Закройте все процессы, работающие с bot.db
3. Перезапустите бота

#### Ошибка: "no such table: sync_state"

**Причина**: База данных не инициализирована

**Решение**:
1. Удалите `data/bot.db` (если существует)
2. Перезапустите бота - БД создастся автоматически

```bash
rm data/bot.db
python run.py
```

#### Проблема: Статистика не обновляется

**Решение**:

1. Проверьте что синхронизации действительно происходят:
   ```
   !rolestats logs 10
   ```

2. Проверьте права на запись в `data/bot.db`

3. Проверьте логи на ошибки БД:
   ```bash
   grep -i "database" logs/bot.log
   ```

### 6. Проблемы с правами

#### Ошибка: "Missing Permissions" при выполнении админ-команд

**Причина**: У пользователя нет прав администратора

**Решение**:

**Вариант 1**: Выдайте себе права администратора на сервере

**Вариант 2**: Добавьте свою роль в список админ-ролей

В `config/config.yaml`:
```yaml
bot:
  admin_role_ids:
    - 123456789012345678  # ID вашей роли
```

**Как получить ID роли**:
1. Включите режим разработчика в Discord
2. Настройки сервера → Роли
3. Правый клик на роли → Копировать ID

### 7. Проблемы производительности

#### Проблема: Медленная синхронизация

**Решение 1**: Увеличьте задержку между синхронизациями

В `config/config.yaml`:
```yaml
sync:
  sync_interval_seconds: 600  # Увеличить с 300 до 600
```

**Решение 2**: Оптимизируйте маппинги

- Удалите неиспользуемые маппинги
- Отключите маппинги через `enabled: false` вместо удаления

**Решение 3**: Используйте более мощный сервер

Требования:
- Минимум 512 MB RAM
- Стабильное интернет-соединение
- Python 3.9+

#### Проблема: Высокое использование RAM

**Причина**: Большое количество серверов/пользователей

**Решение**:
1. Перезапускайте бота периодически (раз в неделю)
2. Очищайте старые логи из БД
3. Используйте PostgreSQL вместо SQLite для больших развертываний

### 8. Логирование и отладка

#### Как включить детальные логи

В `config/config.yaml`:
```yaml
logging:
  level: "DEBUG"  # Вместо INFO
```

Перезапустите бота. Теперь все действия будут подробно логироваться.

#### Где смотреть логи

**В файле**:
```bash
# Linux/Mac:
tail -f logs/bot.log

# Windows PowerShell:
Get-Content logs\bot.log -Tail 50 -Wait
```

**В консоли**:
Логи также выводятся в консоль с цветовой кодировкой.

**В Discord (для админов)**:
```
!rolestats logs 20
```

#### Экспорт логов для поддержки

```bash
# Последние 1000 строк
tail -n 1000 logs/bot.log > debug_logs.txt

# Или через команду (только для админов)
!rolestats export 7
```

## Диагностические команды

### Проверка конфигурации перед запуском

```bash
python validate_setup.py
```

Этот скрипт проверит:
- Версию Python
- Установленные зависимости
- Наличие .env файла
- Корректность config.yaml
- Корректность role_mappings.json
- Структуру проекта

### Проверка прав бота

```
!roleadmin check_permissions
```

Показывает права бота на всех серверах и обнаруженные проблемы.

### Проверка маппингов

```
!roleadmin list_mappings
!roleadmin mapper_stats
```

### Тестирование синхронизации

```
# Синхронизировать себя
!roleadmin sync_user ВАШ_ID

# Посмотреть результаты
!rolestats user @ВЫ

# Посмотреть логи
!rolestats logs 10
```

## Часто задаваемые вопросы

### Q: Можно ли синхронизировать роли с нескольких серверов одновременно?

**A**: Да! Бот автоматически проверяет все сервера где есть и пользователь, и бот. Если пользователь имеет роли на нескольких серверах, все соответствующие роли будут добавлены на главном сервере.

### Q: Что произойдет если удалить роль из маппинга?

**A**: Роль перестанет синхронизироваться, но уже выданные роли не будут автоматически удалены. Для удаления ролей используйте ручную синхронизацию.

### Q: Можно ли синхронизировать роли в обе стороны?

**A**: Нет, бот работает только в одном направлении: с "исходных" серверов на "главный" сервер. Это сделано намеренно для безопасности.

### Q: Сколько маппингов можно создать?

**A**: Технически неограниченно, но рекомендуется не более 100 для оптимальной производительности.

### Q: Будут ли логи занимать много места?

**A**: Нет, логи ротируются автоматически. По умолчанию хранится 5 файлов по 10 MB каждый (макс 50 MB).

### Q: Можно ли использовать бота на Managed/VPS хостинге?

**A**: Да! Используйте `discord-bot.service` для systemd или создайте screen/tmux сессию.

## Получение поддержки

Если проблема не решена:

1. **Соберите информацию**:
   - Версия Python: `python --version`
   - Версия discord.py: `pip show discord.py`
   - Содержимое логов: последние 100 строк из `logs/bot.log`
   - Вывод `python validate_setup.py`

2. **Проверьте документацию**:
   - README.md - общая информация
   - SETUP.md - инструкция по установке
   - Этот файл - устранение проблем

3. **Поиск в Issues**:
   - Проверьте существующие issues в репозитории проекта

4. **Создайте Issue**:
   - Опишите проблему
   - Приложите логи
   - Укажите шаги для воспроизведения

---

**Успешной работы с ботом!** 🤖
