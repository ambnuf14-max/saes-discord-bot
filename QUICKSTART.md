# Быстрый старт

## ⚡ За 5 минут до запуска

### Шаг 1: Установка (2 минуты)

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### Шаг 2: Конфигурация бота Discord (1 минута)

1. Перейдите на https://discord.com/developers/applications
2. Создайте новое приложение или выберите существующее
3. Перейдите в раздел **Bot**
4. Нажмите **Reset Token** → скопируйте токен
5. Включите **Privileged Gateway Intents**:
   - ✅ SERVER MEMBERS INTENT
   - ✅ MESSAGE CONTENT INTENT

### Шаг 3: Настройка проекта (2 минуты)

#### 3.1 Создать .env файл

```bash
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

Открыть `.env` и вставить токен:
```env
DISCORD_BOT_TOKEN=ваш_токен_здесь
ENVIRONMENT=development
```

#### 3.2 Настроить config.yaml

Откройте `config/config.yaml` и измените:

```yaml
bot:
  main_server_id: 123456789012345678  # ← ЗАМЕНИТЬ на ID вашего главного сервера
```

**Как получить ID сервера**:
1. Discord → Настройки → Расширенные → Режим разработчика (включить)
2. Правый клик на сервере → Копировать ID

#### 3.3 Настроить role_mappings.json

Откройте `config/role_mappings.json` и добавьте хотя бы один маппинг:

```json
{
  "mappings": [
    {
      "id": "test_mapping",
      "source_server_id": "ID_ИСХОДНОГО_СЕРВЕРА",
      "source_role_id": "ID_РОЛИ_НА_ИСХОДНОМ_СЕРВЕРЕ",
      "target_server_id": "ID_ГЛАВНОГО_СЕРВЕРА",
      "target_role_id": "ID_РОЛИ_НА_ГЛАВНОМ_СЕРВЕРЕ",
      "description": "Test Role Mapping",
      "enabled": true
    }
  ]
}
```

**Как получить ID ролей**:
1. Discord → Настройки сервера → Роли
2. Правый клик на роли → Копировать ID

### Шаг 4: Пригласить бота на сервера

Сгенерируйте ссылку приглашения:

```
https://discord.com/api/oauth2/authorize?client_id=ВАШ_CLIENT_ID&permissions=268438544&scope=bot
```

Замените `ВАШ_CLIENT_ID` на Application ID из Discord Developer Portal.

**Права бота** (включены в ссылку):
- ✅ View Channels
- ✅ Send Messages
- ✅ Embed Links
- ✅ Read Message History
- ✅ Manage Roles
- ✅ View Server Members

**Важно**: Пригласите бота на:
1. Главный сервер (где будут синхронизироваться роли)
2. Все исходные сервера (откуда берутся роли)

### Шаг 5: Запуск

```bash
# Проверить настройки
python validate_setup.py

# Если все ✅ - запустить бота
python run.py

# Или использовать скрипты:
# Windows:
start.bat

# Linux/Mac:
./start.sh
```

### Шаг 6: Создать кнопку синхронизации

В Discord на главном сервере:

```
!setup_sync_button
```

Готово! Теперь пользователи могут нажимать кнопку для синхронизации ролей.

---

## ✅ Чек-лист готовности

Перед запуском убедитесь:

- [ ] Python 3.9+ установлен (`python --version`)
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Файл `.env` создан и содержит токен бота
- [ ] В `config.yaml` указан правильный `main_server_id`
- [ ] В `role_mappings.json` настроен хотя бы один маппинг
- [ ] В Discord Developer Portal включены Privileged Gateway Intents
- [ ] Бот приглашен на главный сервер
- [ ] Бот приглашен на все исходные сервера
- [ ] Роль бота выше управляемых ролей в иерархии сервера
- [ ] У бота есть право "Manage Roles" на всех серверах

---

## 🎯 Первое тестирование

### 1. Проверить подключение

После запуска вы должны увидеть:

```
==================================================
Бот подключен как: YourBotName (ID: ...)
Серверов: 3
==================================================
  - Main Server (ID: ..., участников: 150)
  - Partner Server 1 (ID: ..., участников: 200)
  - Partner Server 2 (ID: ..., участников: 100)
Главный сервер найден: Main Server
Бот полностью готов к работе!
==================================================
```

### 2. Проверить права

```
!roleadmin check_permissions
```

Должно быть: ✅ Все права в порядке

### 3. Проверить маппинги

```
!roleadmin list_mappings
```

Должны отображаться настроенные маппинги.

### 4. Протестировать синхронизацию

```
# Получите свой ID (включите режим разработчика, правый клик на себя → Копировать ID)
!roleadmin sync_user ВАШ_ID
```

Должно показать результат синхронизации.

### 5. Создать кнопку

```
!setup_sync_button
```

Нажмите кнопку и проверьте работу.

---

## 🚨 Если что-то не работает

### Бот не запускается

```bash
# Проверить настройки
python validate_setup.py

# Посмотреть детальные ошибки
python run.py
```

### Команды не работают

1. Проверьте префикс в `config.yaml` (по умолчанию `!`)
2. Убедитесь что в Developer Portal включен **MESSAGE CONTENT INTENT**
3. Проверьте что у вас есть права Administrator или роль из `admin_role_ids`

### Роли не синхронизируются

1. Проверьте маппинги: `!roleadmin list_mappings`
2. Проверьте права бота: `!roleadmin check_permissions`
3. Убедитесь что роль бота выше целевых ролей в иерархии
4. Посмотрите логи: `!rolestats logs 20`

### Подробнее

См. **TROUBLESHOOTING.md** для детального руководства по устранению проблем.

---

## 📚 Следующие шаги

После успешного запуска:

1. **Настройте дополнительные маппинги**
   - Отредактируйте `config/role_mappings.json`
   - Выполните `!roleadmin reload_config`

2. **Настройте автоматическую синхронизацию**
   - В `config.yaml` установите `auto_sync_enabled: true`
   - Проверьте статус: `!toggle_autosync`

3. **Изучите команды**
   - См. **COMMANDS.md** - полный справочник команд

4. **Настройте мониторинг**
   - Регулярно проверяйте: `!rolestats overview`
   - Экспортируйте статистику: `!rolestats export`

5. **Оптимизируйте производительность**
   - Настройте `sync_interval_seconds` в `config.yaml`
   - Отключите неиспользуемые маппинги

---

## 🔧 Полезные ссылки

- **README.md** - Подробное описание проекта
- **SETUP.md** - Детальная инструкция по установке
- **COMMANDS.md** - Справочник всех команд
- **TROUBLESHOOTING.md** - Решение проблем
- **validate_setup.py** - Скрипт проверки конфигурации

---

## 💡 Советы

1. **Начните с малого**: Создайте 1-2 маппинга для тестирования
2. **Тестируйте на себе**: Используйте `!roleadmin sync_user` перед массовой синхронизацией
3. **Следите за логами**: Периодически проверяйте `!rolestats logs`
4. **Делайте бэкапы**: Копируйте `data/bot.db` перед большими изменениями
5. **Используйте DEBUG режим**: Установите `level: "DEBUG"` в config.yaml для отладки

---

## 🎉 Готово!

Ваш бот синхронизации ролей готов к работе!

Пользователи теперь могут:
- Нажимать кнопку для ручной синхронизации
- Автоматически получать роли при изменениях на других серверах
- Иметь несколько ролей из разных серверов одновременно

Администраторы могут:
- Управлять маппингами через команды или конфиг файлы
- Просматривать детальную статистику
- Выполнять массовые синхронизации
- Экспортировать данные

**Удачи! 🚀**
