# Инструкция по установке и запуску бота

## Требования

- Python 3.9 или выше
- Discord бот токен (получить на [Discord Developer Portal](https://discord.com/developers/applications))
- Бот должен быть добавлен на все сервера с правами:
  - View Channels
  - Send Messages
  - Embed Links
  - Read Message History
  - Manage Roles
  - View Server Members

**Permission Integer для приглашения**: `268438544`

## Шаг 1: Установка зависимостей

```bash
# Создать виртуальное окружение (рекомендуется)
python -m venv venv

# Активировать виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

## Шаг 2: Настройка конфигурации

### 2.1 Создать .env файл

Скопируйте `.env.example` в `.env` и добавьте токен вашего бота:

```env
DISCORD_BOT_TOKEN=ваш_токен_здесь
ENVIRONMENT=development
```

### 2.2 Настроить config.yaml

Откройте `config/config.yaml` и настройте:

```yaml
bot:
  # ID главного сервера (где будут синхронизироваться роли)
  main_server_id: 123456789012345678  # ЗАМЕНИТЬ

  # Префикс команд
  command_prefix: "!"

  # ID канала для кнопки синхронизации (опционально)
  sync_button_channel_id: 234567890123456789  # ЗАМЕНИТЬ

  # ID ролей администраторов (опционально)
  admin_role_ids:
    - 345678901234567890  # ЗАМЕНИТЬ

  # ID канала для логов (опционально)
  log_channel_id: 567890123456789012  # ЗАМЕНИТЬ

sync:
  # Включить автоматическую синхронизацию при изменении ролей
  auto_sync_enabled: true

  # Интервал проверки очереди (секунды)
  sync_interval_seconds: 300

  # Разрешить массовую синхронизацию через команду
  batch_sync_enabled: true

database:
  path: "data/bot.db"

logging:
  level: "INFO"  # DEBUG для детальных логов
  file_path: "logs/bot.log"
  max_bytes: 10485760  # 10 MB
  backup_count: 5
```

### 2.3 Настроить маппинг ролей

Откройте `config/role_mappings.json` и добавьте маппинги:

```json
{
  "mappings": [
    {
      "id": "vip_server_a",
      "source_server_id": "987654321098765432",  // ID исходного сервера
      "source_role_id": "111222333444555666",     // ID роли на исходном сервере
      "target_server_id": "123456789012345678",   // ID главного сервера
      "target_role_id": "777888999000111222",     // ID роли на главном сервере
      "description": "VIP from Server A",
      "enabled": true
    }
  ]
}
```

**Как получить ID:**
1. Включите режим разработчика в Discord (Настройки → Расширенные → Режим разработчика)
2. Щелкните правой кнопкой по серверу/роли → Копировать ID

## Шаг 3: Создание директорий

Убедитесь что существуют директории для данных и логов:

```bash
# Windows (PowerShell)
New-Item -ItemType Directory -Force -Path data, logs

# Linux/Mac
mkdir -p data logs
```

## Шаг 4: Запуск бота

```bash
python run.py
```

При успешном запуске вы увидите:

```
==================================================
Бот подключен как: YourBotName (ID: ...)
Discord.py версия: 2.3.x
Серверов: X
==================================================
  - Server Name 1 (ID: ..., участников: ...)
  - Server Name 2 (ID: ..., участников: ...)
...
Главный сервер найден: Main Server Name
Бот полностью готов к работе!
```

## Шаг 5: Создание кнопки синхронизации

После запуска бота, на главном сервере выполните:

```
!setup_sync_button
```

Или укажите конкретный канал:

```
!setup_sync_button #канал-для-синхронизации
```

Бот создаст сообщение с кнопкой "🔄 Синхронизировать роли".

## Проверка работы

### Тест 1: Проверка прав

```
!roleadmin check_permissions
```

Эта команда проверит права бота на всех серверах.

### Тест 2: Просмотр маппингов

```
!roleadmin list_mappings
```

Покажет все настроенные маппинги ролей.

### Тест 3: Ручная синхронизация

```
!roleadmin sync_user ВАШ_ID
```

Попробуйте синхронизировать себя вручную.

### Тест 4: Кнопка синхронизации

Нажмите кнопку "🔄 Синхронизировать роли" и проверьте результат.

## Команды администратора

### Управление маппингами

```
!roleadmin list_mappings                    # Список всех маппингов
!roleadmin add_mapping <id> <source_srv> <source_role> <target_role> <описание>
!roleadmin remove_mapping <id>              # Удалить маппинг
!roleadmin reload_config                    # Перезагрузить конфигурацию
```

### Синхронизация

```
!roleadmin sync_user <user_id>              # Синхронизировать пользователя
!roleadmin sync_all                         # Синхронизировать всех (требует подтверждение)
!roleadmin mapper_stats                     # Статистика маппингов
```

### Мониторинг

```
!toggle_autosync                            # Вкл/выкл автосинхронизацию
!sync_queue                                 # Показать очередь синхронизации
!clear_sync_queue                           # Очистить очередь
```

## Команды статистики

```
!rolestats overview [дней]                  # Общая статистика (по умолчанию 30 дней)
!rolestats user [@пользователь]             # Статистика пользователя
!rolestats logs [лимит]                     # Последние логи (макс 100)
!rolestats daily [дней]                     # Ежедневная статистика (макс 30)
!rolestats export [дней]                    # Экспорт в CSV (только админ)
```

## Устранение проблем

### Бот не отвечает на команды

1. Проверьте что префикс команд правильный в `config.yaml`
2. Убедитесь что у бота есть права читать сообщения в канале
3. Проверьте логи в `logs/bot.log`

### Ошибка "Недостаточно прав"

1. Выполните `!roleadmin check_permissions`
2. Убедитесь что роль бота выше управляемых ролей в иерархии сервера
3. Проверьте что бот имеет право "Manage Roles"

### Роли не синхронизируются

1. Проверьте маппинги: `!roleadmin list_mappings`
2. Убедитесь что маппинг включен (`enabled: true`)
3. Проверьте что пользователь есть на обоих серверах
4. Проверьте иерархию ролей на главном сервере
5. Посмотрите логи: `!rolestats logs 20`

### База данных повреждена

```bash
# Удалить базу данных (будет создана заново при запуске)
rm data/bot.db

# Или переименовать для сохранения
mv data/bot.db data/bot.db.backup
```

## Логи

Логи сохраняются в:
- **Консоль**: цветной вывод для удобства отладки
- **Файл**: `logs/bot.log` (ротация при достижении 10 MB, хранится 5 файлов)

Уровни логирования:
- `DEBUG`: детальная информация (для разработки)
- `INFO`: обычная информация о работе
- `WARNING`: предупреждения
- `ERROR`: ошибки
- `CRITICAL`: критические ошибки

Изменить уровень можно в `config/config.yaml`:

```yaml
logging:
  level: "DEBUG"  # или INFO, WARNING, ERROR
```

## Безопасность

⚠️ **ВАЖНО:**

1. **Никогда не публикуйте .env файл** с токеном бота
2. Добавьте `.env` в `.gitignore` (уже добавлено)
3. Регулярно проверяйте права бота на серверах
4. Ограничьте доступ к админ-командам через роли в `config.yaml`
5. Регулярно делайте бэкапы базы данных

## Обновление бота

```bash
# Остановить бота (Ctrl+C)

# Обновить зависимости
pip install -r requirements.txt --upgrade

# Перезапустить бота
python run.py
```

## Поддержка

При проблемах:
1. Проверьте логи в `logs/bot.log`
2. Проверьте конфигурацию
3. Убедитесь что все зависимости установлены
4. Проверьте версию Python (должна быть 3.9+)

---

**Бот готов к работе!** 🚀

Для дополнительной информации смотрите [README.md](README.md).
