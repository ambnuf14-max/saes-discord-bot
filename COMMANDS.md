# Справочник команд бота

## Префикс команд

По умолчанию: `!` (настраивается в `config/config.yaml`)

---

## 📋 Команды настройки

### !setup_sync_button [#канал]

**Описание**: Создает сообщение с кнопкой синхронизации ролей

**Права**: Требуется Administrator

**Примеры**:
```
!setup_sync_button
!setup_sync_button #синхронизация
```

---

## 👑 Административные команды

Все команды требуют права Administrator

### !roleadmin

**Описание**: Показывает список всех доступных админ-команд

**Пример**:
```
!roleadmin
```

### !roleadmin sync_all

**Описание**: Синхронизировать роли всех пользователей на главном сервере

**Примечание**: Требует подтверждения, может занять время

**Пример**:
```
!roleadmin sync_all
```

**Процесс**:
1. Бот запросит подтверждение
2. Напишите `да` в течение 30 секунд
3. Бот синхронизирует всех пользователей
4. Выведет статистику (успешно/ошибок/пропущено)

### !roleadmin sync_user <user_id>

**Описание**: Синхронизировать роли конкретного пользователя

**Параметры**:
- `user_id` - ID пользователя Discord

**Пример**:
```
!roleadmin sync_user 123456789012345678
```

**Как получить ID пользователя**:
1. Включите режим разработчика в Discord
2. Правый клик на пользователя → Копировать ID

### !roleadmin list_mappings

**Описание**: Показать все настроенные маппинги ролей

**Пример**:
```
!roleadmin list_mappings
```

**Вывод**:
- ID маппинга
- Исходный сервер и роль
- Целевая роль
- Статус (включен/выключен)
- Описание

### !roleadmin add_mapping <id> <source_server> <source_role> <target_role> <описание>

**Описание**: Добавить новый маппинг роли

**Параметры**:
- `id` - уникальный идентификатор маппинга (строка)
- `source_server` - ID исходного сервера
- `source_role` - ID роли на исходном сервере
- `target_role` - ID роли на главном сервере
- `описание` - описание маппинга (может содержать пробелы)

**Пример**:
```
!roleadmin add_mapping vip_partner 987654321098765432 111222333444555666 777888999000111222 VIP from Partner Server
```

**Примечание**: После добавления маппинг автоматически сохраняется в БД и активируется

### !roleadmin remove_mapping <id>

**Описание**: Удалить маппинг роли

**Параметры**:
- `id` - ID маппинга для удаления

**Пример**:
```
!roleadmin remove_mapping vip_partner
```

### !roleadmin reload_config

**Описание**: Перезагрузить конфигурацию из файлов

**Пример**:
```
!roleadmin reload_config
```

**Когда использовать**:
- После изменения `config/role_mappings.json`
- После изменения `config/config.yaml`
- Не требует перезапуска бота

### !roleadmin check_permissions

**Описание**: Проверить права бота на всех серверах

**Пример**:
```
!roleadmin check_permissions
```

**Проверяет**:
- Наличие права "Manage Roles"
- Позицию роли бота в иерархии
- Доступность целевых ролей для управления

### !roleadmin mapper_stats

**Описание**: Показать статистику по маппингам

**Пример**:
```
!roleadmin mapper_stats
```

**Выводит**:
- Всего маппингов
- Активных маппингов
- Отключенных маппингов
- Уникальных исходных серверов

---

## 🔄 Команды мониторинга

### !toggle_autosync

**Описание**: Включить/выключить автоматическую синхронизацию

**Права**: Требуется Administrator

**Пример**:
```
!toggle_autosync
```

**Примечание**: Изменение НЕ сохраняется после перезапуска. Для постоянного изменения редактируйте `config.yaml`:
```yaml
sync:
  auto_sync_enabled: false
```

### !sync_queue

**Описание**: Показать текущую очередь синхронизации

**Права**: Требуется Administrator

**Пример**:
```
!sync_queue
```

**Выводит**:
- Список пользователей в очереди
- Время до синхронизации для каждого

### !clear_sync_queue

**Описание**: Очистить очередь синхронизации

**Права**: Требуется Administrator

**Пример**:
```
!clear_sync_queue
```

**Примечание**: Используйте если очередь "застряла" или для сброса pending синхронизаций

---

## 📊 Команды статистики

### !rolestats

**Описание**: Показывает список всех доступных команд статистики

**Пример**:
```
!rolestats
```

### !rolestats overview [дней]

**Описание**: Показать общую статистику синхронизации

**Параметры**:
- `дней` - количество дней для анализа (по умолчанию 30)

**Примеры**:
```
!rolestats overview
!rolestats overview 7
!rolestats overview 90
```

**Выводит**:
- Всего синхронизаций
- Успешных синхронизаций
- Ошибок
- Процент успешности
- Уникальных пользователей
- Ролей добавлено/удалено

### !rolestats user [@пользователь]

**Описание**: Показать статистику синхронизации для пользователя

**Параметры**:
- `@пользователь` - упоминание пользователя (опционально, по умолчанию вы сами)

**Примеры**:
```
!rolestats user
!rolestats user @JohnDoe
```

**Выводит**:
- Последняя синхронизация (время)
- Всего синхронизаций
- Последние назначения ролей

### !rolestats logs [лимит]

**Описание**: Показать последние логи синхронизации

**Параметры**:
- `лимит` - количество записей (по умолчанию 20, максимум 100)

**Примеры**:
```
!rolestats logs
!rolestats logs 50
```

**Выводит**:
- Время события
- ID пользователя
- Тип действия (add/remove роли)
- Тип триггера (button/auto/manual)
- Статус (успех/ошибка)
- Сообщение об ошибке (если есть)

**Примечание**: Использует пагинацию для больших списков

### !rolestats daily [дней]

**Описание**: Показать ежедневную статистику

**Параметры**:
- `дней` - количество дней (по умолчанию 7, максимум 30)

**Примеры**:
```
!rolestats daily
!rolestats daily 14
```

**Выводит для каждого дня**:
- Дата
- Всего синхронизаций
- Успешных
- С ошибками
- Процент успешности

### !rolestats export [дней]

**Описание**: Экспортировать статистику в CSV файл

**Права**: Требуется Administrator

**Параметры**:
- `дней` - количество дней для экспорта (по умолчанию 30)

**Примеры**:
```
!rolestats export
!rolestats export 90
```

**Формат CSV**:
```
timestamp,user_id,action_type,trigger_type,success,error_message
2024-01-15 10:30:00,123456789,role_add,button,True,""
```

**Примечание**: Файл отправляется в чат

---

## 🔘 Интерактивные элементы

### Кнопка "🔄 Синхронизировать роли"

**Описание**: Кнопка для ручной синхронизации ролей пользователем

**Как использовать**:
1. Найдите сообщение с кнопкой (создается через `!setup_sync_button`)
2. Нажмите кнопку
3. Дождитесь результата (отобразится только вам)

**Что происходит**:
1. Бот проверяет все сервера, где есть и вы, и бот
2. Собирает ваши роли на этих серверах
3. Сопоставляет роли с маппингами
4. Добавляет соответствующие роли на главном сервере
5. Удаляет роли, которых больше нет
6. Показывает детальный результат

**Результат показывает**:
- ✅ Добавленные роли
- ❌ Удаленные роли
- 📊 Количество проверенных серверов
- ⚠️ Ошибки (если есть)

---

## 🎯 Примеры использования

### Первоначальная настройка

```
# 1. Проверить права
!roleadmin check_permissions

# 2. Посмотреть текущие маппинги
!roleadmin list_mappings

# 3. Добавить новый маппинг
!roleadmin add_mapping vip_server1 987654321098765432 111222333444555666 777888999000111222 VIP from Server 1

# 4. Создать кнопку синхронизации
!setup_sync_button #синхронизация

# 5. Протестировать на себе
!roleadmin sync_user YOUR_ID
```

### Ежедневное использование

```
# Посмотреть статистику за сегодня
!rolestats daily 1

# Посмотреть последние логи
!rolestats logs 20

# Посмотреть очередь
!sync_queue
```

### Отладка проблем

```
# 1. Проверить права
!roleadmin check_permissions

# 2. Посмотреть логи ошибок
!rolestats logs 50

# 3. Посмотреть статистику конкретного пользователя
!rolestats user @ProblematicUser

# 4. Попробовать ручную синхронизацию
!roleadmin sync_user USER_ID

# 5. Перезагрузить конфигурацию
!roleadmin reload_config
```

### Массовое обновление

```
# 1. Добавить/изменить маппинги в role_mappings.json

# 2. Перезагрузить конфигурацию
!roleadmin reload_config

# 3. Синхронизировать всех пользователей
!roleadmin sync_all

# 4. Подтвердить: да

# 5. Проверить результаты
!rolestats overview 1
```

---

## ⚙️ Настройки команд

### Изменение префикса

В `config/config.yaml`:
```yaml
bot:
  command_prefix: "?"  # Теперь команды: ?roleadmin, ?rolestats
```

### Ограничение доступа к админ-командам

В `config/config.yaml`:
```yaml
bot:
  admin_role_ids:
    - 123456789012345678  # Только пользователи с этой ролью
    - 234567890123456789  # или этой
```

**Примечание**: Пользователи с правами Administrator всегда имеют доступ

---

## 📖 Дополнительная информация

- **README.md** - Общая информация о проекте
- **SETUP.md** - Инструкция по установке и настройке
- **TROUBLESHOOTING.md** - Решение типичных проблем
- **config/config.yaml** - Конфигурация бота
- **config/role_mappings.json** - Маппинги ролей

---

**Удачи в использовании бота!** 🚀
