# Конфигурация Discord Role Sync Bot

bot:
  # ID главного сервера (ОБЯЗАТЕЛЬНО: замените на ID вашего сервера)
  main_server_id: 123456789012345678

  # Префикс для команд
  command_prefix: "!"

  # ID канала для размещения кнопки синхронизации
  # Найдите канал, ПКМ -> Копировать ID канала
  sync_button_channel_id: 123456789012345678

  # ID ролей администраторов (могут использовать админ-команды)
  # Оставьте пустым [] если хотите разрешить только владельцу сервера
  admin_role_ids:
    - 123456789012345678

  # ID канала для Discord логирования (ОПЦИОНАЛЬНО)
  # Оставьте пустым или удалите если не нужно
  log_channel_id: 123456789012345678

sync:
  # Включить автоматическую синхронизацию при изменении ролей
  auto_sync_enabled: true

  # Интервал проверки изменений (в секундах)
  sync_interval_seconds: 300

  # Разрешить массовую синхронизацию всех пользователей
  batch_sync_enabled: true

database:
  # Путь к файлу базы данных SQLite
  path: "data/bot.db"

logging:
  # Уровень логирования: DEBUG, INFO, WARNING, ERROR, CRITICAL
  level: "INFO"

  # Путь к файлу логов
  file_path: "logs/bot.log"

  # Максимальный размер файла логов в байтах (10MB)
  max_bytes: 10485760

  # Количество резервных копий файлов логов
  backup_count: 5
